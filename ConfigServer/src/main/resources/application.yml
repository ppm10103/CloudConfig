server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ppm10103/CloudConfig.git
          clone-on-start: true
          skip-ssl-validation: true
